name: bmad-flow
version: 0.1.0
description: >
  Session lifecycle, design/sketch, and sync-check agents for BMAD.
  Adds structured session rituals (with auto-harvest ADRs), HTML sketch
  generation, and design system management on top of BMAD Core.
